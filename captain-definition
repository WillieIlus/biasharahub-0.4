{
  "schemaVersion": 2,
  "dockerfileLines": [
    "FROM library/python:3.6.3-alpine",
    "RUN apk update && apk upgrade && apk add --no-cache make g++ bash git openssh postgresql-dev curl",
    "RUN mkdir -p /usr/src/app",
    "WORKDIR /usr/src/app",
    "COPY ./biasharahub/ /usr/src/app",
	"RUN apk update && apk add --virtual build-deps gcc python3-dev musl-dev && pip install psycopg2 && apk add jpeg-dev zlib-dev libjpeg && pip install Pillow && apk del build-deps"
    "RUN pip install -r requirements.txt",
    "COPY ./utils/ /usr/src/utils",
    "EXPOSE 80",
    "CMD sh /usr/src/utils/run.sh"
  ]
}
